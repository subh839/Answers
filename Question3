<!DOCTYPE html>
<html>
<body>

<script>

// Ascending order
var array1= [ ];
// data type array name 
array1.sort((a,b) => {
    if(!a && !b ) return [];
    if(a > b) return 1;
    if(a < b) return -1;
    return 0;
});
console.log(array1);
var array2= [1];
array2.sort((a,b) => {
    if(a > b) return 1;
    if(a < b) return -1;
    return 0;
});

console.log(array2);

var array3= [2, 4, 7, 6, 6, 6, 5, 8, 9];
array3.sort((a,b) => {
    if(a > b) return 1;
    if(a < b) return -1;
    return 0;
});

console.log(array3);

// descending order 
var array4= [];
array4.sort((a,b) => {
    if(a < b) return 1;
    if(a > b) return -1;
    return 0;
});

console.log(array4);

var array5= [1];
array5.sort((a,b) => {
    if(a < b) return 1;
    if(a > b) return -1;
    return 0;
});

console.log(array5);

var array6= [2, 4, 7, 6, 6, 6, 5, 8, 9];
array6.sort((a,b) => {
    if(a < b) return 1;
    if(a > b) return -1;
    return 0;
});

console.log(array6);

</script>

</body>
</html>



Only js functons:

// Ascending order
var array1= [ ];
// data type array name 
array1.sort((a,b) => {
    if(!a && !b ) return [];
    if(a > b) return 1;
    if(a < b) return -1;
    return 0;
});



// descending order 
var array4= [];
array4.sort((a,b) => {
    if(a < b) return 1;
    if(a > b) return -1;
    return 0;
});
